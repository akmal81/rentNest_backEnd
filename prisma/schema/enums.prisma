enum Role {
    TENANT
    LANDLORD
    ADMIN
}

enum ActiveStatus {
    ACTIVE
    BLOCK
}

enum PropertyAvailablity {
    AVAILABLE
    BOOKED
    RENTED
    MAINTENANCE
}

enum Availablity {
    YES
    NO
}

enum RentRequestStatus {
    PENDING
    APPROVED
    REJECTED
}


enum PaymentStatus {
    PENDING
    ACTIVE
    FAILED
    CANCELED
}
