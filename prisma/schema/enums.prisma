enum Role {
    TENANT
    LANDLORD
    ADMIN
}

enum ActiveStatus {
    ACTIVE
    BLOCK
}

enum PropertyAvailablity {
    AVAILABLE
    BOOKED
}

enum Availablity {
    YES
    NO
}

enum RentRequestStatus {
    PENDING
    APPROVED
    REJECTED
}
