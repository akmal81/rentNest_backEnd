enum Role {
    TENANT
    LANDLORD
    ADMIN
}

enum ActiveStatus {
    ACTIVE
    BLOCK
}

enum PropertyAvailablity {
    AVAILABLE
    BOOKED
    RENTED
    MAINTENANCE
    DELETED
}

enum Availablity {
    YES
    NO
}

enum RentRequestStatus {
    PENDING
    APPROVED
    REJECTED
    COMPELETED
}

enum PaymentStatus {
    PENDING
    ACTIVE
    FAILED
    CANCELED
}

enum ReviewStatus {
    APPROVED
    REJECTED
}
